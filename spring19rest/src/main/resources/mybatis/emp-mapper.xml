<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="emp">

	<!-- 사원등록 -->
	<select id="sequence" resultType="EmpDto">
		select * from emp 
	</select>
	<insert id="save">
		insert into emp(
			emp_no, emp_name, emp_dept, emp_date, emp_sal
		)
		values(
			#{empNo}, #{empName}, #{empDept}, #{empDate}, #{empSal}
		)
	</insert>

	<!-- Read -->
	
	<!-- 목록조회 -->
	<select id="list" resultType="EmpDto">
		select * from emp order by emp_no asc
	</select>
	
	<!-- 상세조회 -->
	<select id="find" resultType="EmpDto">
		select * from emp where emp_no = #{empNo}
	</select>
	
	<!-- 
		Update : 전체변경과 항목변경으로 나눠짐 
		<set> 키워드 내부에 <if>를 작성하면 참인 항목만 생성되며 콤마가 조절됨
	-->
	
	<update id="editAll">
		update emp
		set
			emp_name=#{empName},
			emp_dept=#{empDept},
			emp_date = #{empDate},
			emp_sal = #{empSal}
		where emp_no = #{empNo}
	</update>
	<update id="editUnit">
		update emp
		<set>
			<if test="empName != null">emp_name=#{empName},</if>
			<if test="empName != null">emp_dept=#{empDept},</if>
			<if test="empName != null">emp_date = #{empDate},</if>
			<if test="empSal > 0">emp_sal = #{empSal},</if>
		</set>
		where emp_no = #{empNo}
	</update>

	<!-- Delete -->
	<delete id=""></delete>

</mapper>







